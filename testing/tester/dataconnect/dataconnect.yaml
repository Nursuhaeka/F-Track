specVersion: "v1"
serviceId: "development"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "development-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
